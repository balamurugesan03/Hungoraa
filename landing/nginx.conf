server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # $uri.html lets /how-it-works serve how-it-works.html
        try_files $uri $uri.html $uri/ /index.html;
    }
}
